# Production requirements
# Base dependencies are in requirements-base.txt (cached separately)
# This file includes base + any app-specific additions

# Include base requirements
-r requirements-base.txt

# App-specific dependencies (add here if needed)
# Most dependencies are in requirements-base.txt for better caching
