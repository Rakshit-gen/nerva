# Core Framework
fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0

# Database
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Redis & Job Queue
redis==5.0.1
rq==1.16.0

# Vector Database
qdrant-client==1.7.3

# Content Extraction
httpx==0.26.0
beautifulsoup4==4.12.3
youtube-transcript-api==0.6.2
# PyMuPDF==1.23.21  # Optional: PDF extraction (has compilation issues on some systems)

# Text Processing
sentence-transformers==2.3.1
tiktoken==0.5.2

# LLM
huggingface-hub>=0.20.0,<1.0  # Compatible with transformers, will upgrade separately if needed

# Text-to-Speech
TTS==0.22.0  # Only needed if using local TTS (fallback)
# API-based TTS uses httpx (already included) - no additional dependencies needed

# Audio Processing
pydub==0.25.1

# Image Generation (using HF API, diffusers optional for local)
Pillow>=10.3.0

# Utilities
python-dotenv==1.0.1
python-multipart==0.0.9
boto3==1.34.34  # AWS S3 storage
psutil==5.9.8  # Memory monitoring

# Development
pytest>=7.0.0,<8.0.0
pytest-asyncio==0.23.4
black==24.1.1
isort==5.13.2
